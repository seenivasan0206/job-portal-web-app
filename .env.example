# Copy this file to .env and fill in your real values.
# NEVER commit the real .env file.
# All values below are placeholders — there are no insecure defaults in code.

# Flask
FLASK_SECRET_KEY=change-me-to-a-long-random-string-at-least-32-chars
FLASK_DEBUG=0

# MySQL database
DB_HOST=localhost
DB_USER=jobportal
DB_PASSWORD=your-db-password-here
DB_NAME=jobportal_db

# Gmail (used to send OTP emails) — use a Google App Password, NOT your main password
EMAIL_ADDRESS=ccubetech00@gmail.com
EMAIL_PASSWORD=oxyj tfhe xqer qrt

# Upload limits
MAX_UPLOAD_MB=5

# Rate limiting storage (Use Redis in multi-worker/production environments)
# Local single-process default: memory://
# Production multi-worker: redis://localhost:6379/0 or redis://redis:6379/0
RATELIMIT_STORAGE_URI=redis://localhost:6379/0
